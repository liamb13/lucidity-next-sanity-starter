{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["SANITY_STUDIO_*", "SENTRY_*"],
      "outputs": [".sanity/**"]
    },
    "generate:types": {
      "outputs": ["sanity.types.ts", "schema.json", "types/sanity-queries.ts"]
    }
  }
}
